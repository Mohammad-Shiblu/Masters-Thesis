# Core scientific packages
h5py
numpy
scipy
matplotlib
scikit-image
tqdm
requests
pillow

# Deep learning utilities (torch itself is pre-installed on the remote machine)
pytorch-msssim
torchmetrics
lpips

# Baseline
bm3d

# NOTE: torch_radon must be installed separately via  scripts/setup_environment.sh
# (unmaintained; requires compiling from source with CUDA/PyTorch compatibility patches)
