{
    "_description": "Naive cascade (small, end-to-end) — two identical U-Nets [32,64,128,256] ~15M total, trained jointly with gradients flowing through both stages. Controls for the detach variant: rules out the claim that naive cascading only fails because the stages are trained independently. If e2e also fails, joint optimisation alone cannot rescue the naive architecture.",

    "trainer": "deep_supervision",

    "test_no": "lodopab_naive_cascade_e2e_01",
    "model": "lodopab_deep_supervision",

    "data_path": null,

    "num_stages": 2,
    "stage0_features": [32, 64, 128, 256],
    "stage1_mode": "direct",

    "dual_domain": false,
    "detach_stages": false,

    "add_artifacts": true,
    "val_artifact_seed": 42,
    "test_artifact_seed": 0,

    "batch_size": 16,
    "num_workers": 8,

    "lr": 3e-4,
    "epochs": 150,
    "patience": 15,

    "checkpoints_dir": "checkpoints",
    "output_dir": "output"
}
