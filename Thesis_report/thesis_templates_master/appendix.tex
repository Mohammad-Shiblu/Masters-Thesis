\chapter{Additional Visual Results}
\label{app:visuals}

% Include additional qualitative comparisons that support but would interrupt
% the main text. Each figure must be referenced from Chapter 5.
% Suggested content:
%   - Full test-set examples across all artifact types (noise-only, motion,
%     ring, metal, compound).
%   - Per-artifact PSNR/SSIM breakdown table.

\chapter{Implementation Details and Hyperparameters}
\label{app:implementation}

% Provide a complete hyperparameter table: architecture (channels, depth),
% training (LR, batch size, epochs, optimizer betas, weight decay),
% corruption protocol (I_0, motion amplitude, ring column fraction,
% metal object count and size).
% Reference the public GitHub repository for code.

\chapter{Corruption Protocol Parameter Tables}
\label{app:protocol_params}

% Full parameter specification for the corruption protocol from Chapter 3.
% Include the probability of each artifact type being applied per sample,
% severity distributions, and random seeds used for reproducibility.
